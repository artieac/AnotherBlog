services:
  anotherblog:
    container_name: anotherblog
    image: artieac/anotherblog:latest
    ports:
      - '8080:8080'
    restart: unless-stopped
    volumes:
      - ./dataprotection-keys:/app/DataProtection-Keys

networks:
  default:
    external: true
    name: nginx-network
